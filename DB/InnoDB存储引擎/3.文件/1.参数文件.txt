MySQL数据库参数可以分为两类：动态和静态，动态参数在实例的声明周期中可以修改，静态参数不能修改。

静态参数，如read only,只能在配置文件中修改，启动实例事生效；

动态参数可以在实例的运行过程中修改，但是作用域有所差别：
有些动态参数只能在回话中修改，如autocommmit；
有些参数在整个声明周期中都会生效，如binlog_cache_size；
有些动态参数可以在会话中生效，有能在整个实例的生命周期中生效，如read_buffer_size；